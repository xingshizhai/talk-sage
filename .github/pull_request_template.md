## 变更内容

<!-- 用一两句说明本次改了什么、解决什么问题 -->

## 关联 Issue

<!-- 修复 #123 / 关联 #456（没有可删） -->

## 测试

- [ ] `cargo test --workspace`（真实模型测试在无 `models/` 时自动跳过）
- [ ] `cd web && npx vitest run`
- [ ] 前端 `npm run build` 通过
- [ ] 手动验证（如有 GUI/CLI 改动）

## 影响范围

<!-- 是否改动领域事件/DomainEvent、SQLite schema、IPC 命令等跨层接口？（如有请说明兼容处理） -->

## 截图（可选）

<!-- GUI 改动请附截图 -->
