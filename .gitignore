# 本机环境覆盖（每台开发机各自维护，不入库）
scripts/talksage.local.ps1
scripts/talksage.local.sh

# Python (legacy v1 — 已删除，保留条目防止残留)
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# Rust (v2)
target/
.cargo-home/
.tools/

# Frontend (v2)
node_modules/
web/dist/
web/src-tauri/gen/
.npm-cache/

# 模型与测试临时产物（由 scripts/download_models.py 下载）
models/
.pytest_tmp/
evaluation/corpus/**/*.wav
evaluation/reports/

# 分析用临时目录
_meetily_src/

# 开发数据目录（配置、会话数据库、录音；不入库，不随 clean 删除）。
# v0.2+ 目录分离：config/ 只放配置文件，数据在 data/、日志在 logs/。
config/talksage.toml
config/sessions.db
config/logs/
config/recordings/
config/voiceprints/
config/tmp/
config/models/
config/window.json
config/session-*.md
config/exports/
# 数据与日志（与配置分离；sessions.db/录音/导出/声纹在 data\，日志在 logs\）
data/
logs/

# Misc
dist/
.env

tmp/
.DS_Store

# Claude Code 工作区（隔离 worktree 与本地设置，不入库）
.claude/
