# Python (legacy v1 — 已删除，保留条目防止残留)
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# Rust (v2)
target/
.cargo-home/
.tools/

# Frontend (v2)
node_modules/
web/dist/
web/src-tauri/gen/
.npm-cache/

# 模型与测试临时产物（由 scripts/download_models.py 下载）
models/
.pytest_tmp/
evaluation/corpus/**/*.wav
evaluation/reports/

# 分析用临时目录
_meetily_src/

# 开发数据目录（配置、会话数据库、录音；不入库，不随 clean 删除）。
# 脚本默认配置目录为项目内 config/（模板 config/talksage.example.toml 入库，其余运行时产物忽略）
config/talksage.toml
config/sessions.db
config/logs/
config/recordings/
config/voiceprints/
config/tmp/
config/models/

# Misc
dist/
.env

tmp/
.DS_Store

# Claude Code 工作区（隔离 worktree，不入库）
.claude/worktrees/
